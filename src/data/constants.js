import shippingTypes from "./shippingTypes.json";

const sizeTypes = {
  SMALL: "small",
  MEDIUM: "medium",
  LARGE: "large",
};

const cheeseTypes = {
  YES: "yes",
  NO: "no",
};

export { shippingTypes, sizeTypes, cheeseTypes };
